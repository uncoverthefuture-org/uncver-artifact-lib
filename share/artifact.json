{
  "name": "uncver-share",
  "version": "1.0.0",
  "description": "P2P artifact sharing via WebSocket - sends artifacts to friends",
  "language": "cpp",
  "build_type": "cmake",
  "author": "uncver",
  "repository": "https://github.com/uncoverthefuture-org/uncver-artifact-lib/tree/main/share",
  "entrypoint": "uncver-share",
  "environment": {
    "REDIS_HOST": "localhost",
    "REDIS_PORT": "6379"
  },
  "required_artifacts": [
    {
      "name": "uncver-redis-stream",
      "required": true
    }
  ],
  "build": {
    "dependencies": ["cmake", "make", "g++"],
    "steps": [
      "mkdir -p build",
      "cd build && cmake ..",
      "cd build && make -j$(nproc)"
    ]
  },
  "package": {
    "files": ["uncver-share"],
    "docker": false
  }
}
